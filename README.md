redis
=====

This is a redis clone made for a code challenge. It implements basic commands such as set, get, unset, begin, commit, rollback, and end. This app runs on the command line and can be started by:

```ruby init.rb```
